name: touch_interceptor
description: A widget that intercept touch events and send them to other widgets underneath.
version: 0.0.1
author: Daniele Paradiso <parsodyl@gmail.com>
homepage: https://github.com/parsodyl/touch_interceptor

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

