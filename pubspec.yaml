name: touch_interceptor
description: A widget that intercepts touch events and sends them to other widgets underneath.
version: 0.0.2
homepage: https://github.com/parsodyl/touch_interceptor

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

